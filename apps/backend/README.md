# MarkWise Backend

Fastify API service for MarkWise.

## Development

```bash
npm install
npm run dev
```

The server listens on `http://localhost:4000` by default. Check `GET /health` to verify it is running.

## Production

```bash
npm run build
npm start
```
