{
  "name": "markwise",
  "private": true,
  "packageManager": "npm@10.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "prisma:format:backend": "npm exec --workspace=@markwise/backend -- prisma format"   
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "workspaces": [
    "apps/web",
    "apps/backend"
  ]
}
